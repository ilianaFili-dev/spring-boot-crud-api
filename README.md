# spring-boot-crud-api
A complete REST API built with Spring Boot, demonstrating CRUD operations with a 4-layer architecture (Controller, Service, Repository, Entity). Features H2 in-memory database, input validation, and comprehensive logging.
